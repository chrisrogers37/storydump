# Consolidated design plan — multi-tenant storydump (2026-08-02)

**Status:** ratified (FC-0..FC-9, D1–D41) and **in execution** — see *Live status* below for the per-increment position as of 2026-09-04; Phases 0, F and L are built, the target tier is live in production on a greenfield database, Phase X.3 is the current increment. The design-pass history that follows is retained as the record of how the plan reached ratification. Third design pass applied (R3 fresh-session implementer review, comment 4840492901 — its 7-item ratification gate in full: DDL replays from empty as printed, raw-SQL invariant tests threaded into the owning increments' gates, the non-escalatable credential model (`02` §7) + durable pacing/admission schema (`rate_counters`), the named defect fixes, platform-fact conditioning (evidence-authority-parameterized reconciliation contract behind 0.4), the egress floor moved to L.0, and the email + archive operational paths). **Fourth design pass applied (2026-08-03):** the #732 credential-liveness fix (D31); the 2026-08-03 rulings folded in as FC-5/FC-6 — Google-only sign-in with the OTP machinery removed, ruled invitation delivery (email + Telegram on existing machinery) with the D33 per-provider acceptance model and the D36 elevation gate; and the plan **anchored to the codebase** (§Codebase anchor below) — every current-repo claim verified against `main` @ `2e13f97`, disagreements resolved in the code's favor and recorded, most materially the Meta publish cap corrected 25→100 against Meta's primary documentation. **Open for the product owner: fork PA-1 in `03` (implementers build default (a) until ruled); the email-provider ack (reopened by FC-6 — Resend as swappable default meanwhile); the Google consent-screen publishing status — the last two are `03` pass-4 items. D28 (Cloudinary signed-params for FC-3.4) was ratified 2026-08-03, conditional on clean delivery.** **Fifth design pass applied (2026-08-04):** the Codex R4 review dispositioned (comment 5174671536 — anchor layer held; the failures were in conclusions and the execution contract) and three product rulings folded in as FC-7 (offline cutover: days-scale window, credentials re-authenticate by hand, Telegram-works-after as end state, the queue thrown out) and FC-8 (full cloud, media sources as a pluggable adapter surface — D37) plus the FC-3.2 amendment (transit TTL attaches to the asset — D38, with its revisit trigger). `04` is rewritten to the offline shape (Phases W and G deleted, Phase M added); the R4 executable-schema gap is closed in `02`/`07` (transition seeds, roles/policies/RLS-at-creation, all nine door bodies, the retained-table DDL, `runner adopt`), and the retry-debit, restore, invitation-door, and username-proof defects are fixed in place. **Sixth design pass applied (2026-08-04): the Codex R5 review dispositioned — R5 executed the plan's SQL and every finding was a runtime failure; each was reproduced on PostgreSQL 15 before fixing and re-verified after.** The two P0s: the cutover's sequencing gap closed by the M.3 schema move (D39 — legacy `public` renamed aside so the F.2 files meet an empty schema by construction, with an in-place rollback lever until the final drop) and the governance audit trigger restructured to statement-nested table dispatch (the AND-chained early-exits bricked every governance table — PL/pgSQL resolves record fields at expression setup, which short-circuit does not prevent). The P1s: `fn_invitation_accept` un-broken (o_-prefixed door outputs — the output column shadowed `workspace_id` in its ON CONFLICT list), the `command_dedup` sweep grant added, archive batch names collision-proofed (`clock_timestamp()` + microseconds), `fn_next_slot` rewritten to index construction (the 7,344/28,800 extra-boundary-slot scan now returns 0/28,800) with timezones gated at write (`fn_safe_tz` CHECKs) and degraded per-row at read (one bad zone no longer rolls back a whole tick), and the published aggregate bounds made structural in the door bodies (the tick's `p_max` and the reconciler's `p_lim` are total budgets by construction). The P2 ground-rule/0.3 contradiction closed by naming the two pre-window exceptions, and `advertised_ddl_replay` redefined so the mechanical policy expansion is part of the advertised stream and the F.2 files are held equal to it by diff (the gate now tests exactly what production runs). **Seventh design pass applied (2026-08-05): the Codex R6 review dispositioned** — all three findings reproduced under the production actor on a throwaway PG 15.18 before any fix; D40 (the window privilege split, with its transient-placement rule) and D41 (the runner-retryable four-leg rollback and its ledger boundary) are the pass's decisions. **Eighth design pass applied (2026-08-05): the Codex R7 review dispositioned** — per-path stand-down variants with runnable property gates, the bootstrap owner precondition, and the 0.2 PG16+ actor/ADMIN contract; the pass's lesson is recorded in D40 and applied at point of use: *a gate must assert the property it exists to guarantee, not the mechanism that was supposed to produce it.* **Ninth design pass applied (2026-08-06): the Codex R8 review dispositioned** — subject-identity guards on both stand-down variants (a gate verifies its subject before asserting properties over it), viewer-independent `pg_catalog` gate predicates, and the membership invariant restated version-aware after the pass-8 pair proved mutually unsatisfiable on PG16+. **FC-9 recorded (ruling 2026-08-07): storydump is a hosted product we operate and charge for** — a codification of what the plan already assumed throughout, not a change of direction; no mechanism moves, and `06` §6's billing deferral becomes a sequencing decision under a known model.
**Supersedes:** the standalone adoption of either prior package (see Authority, below).
**Executable by:** an implementer working increment-by-increment from `04-execution-sequence.md` with zero design judgment calls — every shape, key, state, number, and gate is stated here or explicitly incorporated by path. **The claim's exact bounds, stated rather than softened (pass 5 — R4 held this claim to its own standard, and the standard is kept):** *design* is fully decided — no product choice, schema shape, invariant, or sequencing decision is left open below the gates named in Live status; what an implementer still *authors* is implementation with the design in hand: service and adapter code against the printed contracts (the Drive adapter's API plumbing behind the `01` port; executors for the `02` §5 registry), the M.1 transform SQL written to the stated per-table rules and postconditions (the rules leave no choices open; the SQL is theirs to type), and tests to the stated gates. Two classes of genuinely open items gate narrow slices and are marked where they bite: the owner items in Live status (each with its stated default or halt), and the 0.4 platform verdicts (each behind its config seam).

## Live status (position tracker — updated as work proceeds, not at stop time)

**Position as of 2026-09-04** (the 2026-09-02 audit against `main`, the tracker, and the recorded production probes — #1212 — refreshed after the builds of 2026-09-03/04): the plan is ratified and **in execution**. Phases 0, F and L are built; the target tier serves production on a **greenfield** database (sign-in, workspace create and the clock are live — #1090); Phase M ran by hand rather than by its runbook and is not closed out; **Phase X.3 is the current increment**; Phase S is untouched. The pass-1..9 ratification record is unchanged below.

- **Ratified:** FC-0..FC-9 (rulings, incl. the FC-3.2 amendment); D1–D41 per `03`, including D28 (conditional on clean delivery), D38 (scoped to launch scale with its stated revisit trigger), and D40/D41 (the window privilege split and the rollback ledger boundary — pass 7; amended pass 8 with the both-exits closure rule, pass 9 with subject-identity gating and the version-aware membership property). Since: the email-provider deferral (owner, 2026-09-01 — `03` pass-4 items; `04` X.3 gate clause 3, #1201) and the clause-6 object correction (#1181, #1193).
- **Open (product owner):** PA-1 — implementers build default (a) until ruled; the Google consent-screen publishing status (console fact); clause 5's arming decision (#1127); the return route for inviting a non-user (unruled, #1201); Drive cardinality (#1168). The three M.1 proposed drops are moot under ruling 1 below.
- **Rulings applied in practice, confirmed by the owner on 2026-09-02 and recorded as `00` FC-7 §6–§8 and `03` §Post-ratification rulings** (the `04` M-phase text carries pointer lines):
  1. **Legacy data is not migrated (confirmed 2026-09-02).** No M.1 transform file was ever written and none will be; the target went live greenfield and the legacy lineage sits untransformed in the `legacy` schema (first recorded second-hand at #1171 §3 and #1090 G16). M.1 is abandoned, 0.3 (#695) and the M.1 measurements (#943) are closed as moot, the proposed drops are moot, and 3f/3g remain owed.
  2. **The M.3 window ran by hand, not by the runbook.** Steps 3a–3d were applied to production as `neondb_owner` across 2026-08-24 and 2026-08-26 (#1195, #1014) — not as `svc_migration` in one advisory-locked run; the target worker was armed 2026-08-24 (`WORKER_IMPL=target`, #1014) and the API cut over to `api.storydump.app` on 2026-08-31 (#1090). No M.2 rehearsal, no window log, no step-8 stand-down.
  3. **The parity bar (FC-7.3) was not a precondition in practice.** The web queue is the act-on-it surface (#1033) and Telegram a second channel (#1090, G8). FC-7 §3's *"Telegram works after"* end state is still owed — general chat-inbound dispatch is #854.
- **Schedule gate — lifted (owner ruling 2026-08-18):** the M.3 window no longer waits for 0.1 App Review approval; FC-7.5's ordering constraint is removed while 0.1 itself **remains required** — it gates the owner's post-cutover reconnect, the app staying in its current integration state until approval lands (`00` FC-7 §5 carries the ruling verbatim).
- **Next:** drive X.3 clauses 1 and 4 against the now-real surfaces (#1157, #1172 — Instagram connect #1221 and the Telegram link #1224 are on `main`; the webhook registration is the owner's step, `documentation/operations/telegram-webhook.md`); decide clause 5's arming (#1127 — delete/restore built, #1219); switch production to the F.4 runtime login (#751 — `/health` now reports which login it holds, #1218); #1220 step 3, the publish-media leg; schedule 3f/3g behind #410 (#1202). Done since 2026-09-02: the 0.2 predeploy is armed (#1217).

### Increment scoreboard (2026-09-04)

Legend: ✅ built, gate met · 🔧 in progress · 🟡 built, never driven · 🟠 built, unreachable · ⏸️ deferred / not run · 📋 pending · ⛔ deleted tombstone, abandoned or moot. "✓" after a tracker number means the issue is closed.

| Increment | Tracker | State | Evidence and what remains |
|---|---|---|---|
| 0.1 App Review submission | #410 | 🔧 blocked on Meta | runbook (#1203) and the policy callbacks (#1208) merged 2026-09-02; Business Verification not started; gates the owner's Instagram-Login reconnect (FC-7 §5) |
| 0.2 Migration runner | #712 ✓ | ✅ built · ✅ rolled out | `scripts/migration_runner.py`; production entered the ledger 2026-08-26 (046–050 adopted, 051+ applied). Runbook step 5 taken 2026-09-03: `railway.toml` `preDeployCommand` runs `migration_runner apply` on every deploy (#1217, closes #1195); the scheduled drift monitor (#1197) stays as the independent check |
| 0.3 Production duplicate remediation | #695 ✓ | ⛔ moot | its purpose was clean inputs for the M.1 transform; closed 2026-09-02 under ruling 1 |
| 0.4 Meta primary-doc verification | #745 ✓ | ✅ | `0.4-meta-primary-doc-verification.md` (2026-08-13) |
| 0.5 Window legacy-DDL door | #787 ✓ | ✅ | `scripts/window/step0_legacy_ddl_door.sql` (#934) |
| F.1 Fail-closed tenant interfaces | #841 | 🔧 | `src/repositories/tenant_scope.py` (#846); keyword-only tenant params 22 of 72 (#962); 8 `if chat_settings_id` sites unclassified; web-boundary dependency #869. All on the legacy repositories, which retire with the legacy tier |
| F.2 Target schema as runner migrations | #806 | ✅ | 9 of 9 segments = migrations 052–060 (merged 2026-08-19, applied to production); the tracker title still says 2 of 9 |
| F.3 Neutral tenant resolution | #842 ✓ | ✅ | `src/services/target/tenant_resolution.py` (#856, #866) |
| F.4 RLS harness | #751 | 🔧 | harness built (#789, #852 — 58 policies exercised as the declared logins). **Production still connects as `neondb_owner`: `BYPASSRLS`, owner of all 26 tables (measured 2026-08-25)**, so every `p_tenant` policy is inert on the deployed path; the runtime-login posture F.4 exists to install is not in place. Since 2026-09-03 `/health` reports `db_role` and whether it bypasses RLS (#1218, with the F.4 runbook `documentation/operations/runtime-database-roles.md`), so the switch is observable when the owner makes it |
| F.5 | — | ⛔ | FC-7 tombstone |
| F.6 FC-2 ratchet install | #843 ✓ | ✅ | `scripts/telegram_ratchet.py`; committed baseline core segment = 15 modules (X.1 burns it down) |
| L.0 – L.8 | #857–#865 ✓ | ✅ | all merged 2026-08-19/20 (L.5 executor body #939); open follow-ups #1112 (`classify_error` default), #1010 (`WORKER_IMPL` DB-URL fallback) |
| L.9 | — | ⛔ | FC-7 tombstone |
| M.1 Transform spec | #790 · #943 ✓ | ⛔ abandoned | spec at `../2026-08-17-m1-transform-spec/` retained as the record; no 3e file written; legacy data not migrated (ruling 1); #943's measurements closed as moot |
| M.2 Rehearsal | #790 | ⏸️ not run | spec at `../2026-08-17-m2-rehearsal-spec/`; overtaken by the hand-applied window (ruling 2); its success/abandon stand-down legs still govern 3g |
| M.3 The window | #790 · #1202 | 🔧 partial | 3a–3d ✅ by hand (ruling 2); 3e ⛔ abandoned; 3f snapshots and 3g `DROP SCHEMA legacy` ✗ — 3g is blocked on #410's demo videos (#1202) and nothing enforces that; step 4 ✅ (worker 2026-08-24, API 2026-08-31); step 5 owner re-auth ✗ (no credentialed account connected; the Instagram-Login reconnect follows #410 per FC-7 §5); step 6 smoke partial (#1090: sign-in and workspace create observed, no post yet); step 7 clock ✅ (live since 065, 2026-08-26); step 8 stand-down ✗ (legacy schema and owner-role privileges still live); no window log committed |
| Parity bar (FC-7.3) | #854 | ⏸️ | `../2026-08-17-m3-parity-bar-mapping/`; ruling 3 |
| X.1 Core-empty milestone | — | 📋 | core segment at 15 (`scripts/telegram_ratchet_baseline.json`); no burn-down PR yet |
| X.2 Web surface parity + first-party API auth | — | 🔧 | web queue and the closed command vocabulary over the API ✅ (#1033, #1035); the `review_required` operator surface ✗ (`resolve_review` / `clear_quarantine` unbuilt — #1090 D6); `service_tokens` table exists (060) but the CLI still imports `src.services.*` directly — CLI-over-API not started |
| X.3 Multi-workspace UX enablement | #1172 | 🔧 current | gate: clauses 2 and 6 ✅; clause 3 deferred out (#1201); clauses 1 and 4 🟡 built and now REAL — Instagram connect per destination (#1221) and the Telegram link route + Settings card (#1224), replacing the type-a-handle stand-in — never driven (#1157 is the session); clause 5 🟠 delete/restore built (#1219), arming decision pending (#1127). Off-gate X.3 items: `move_account`, `change_role`, `remove_member`, `transfer_ownership` unbuilt; `pause_workspace` / `resume_workspace` built but unreachable (#1167) |
| S.1 Load harness + telemetry | — | 📋 | gate scenarios exist under `tests/scripts/`; no versioned load harness |
| S.2 Admission, pg-shaped | — | 📋 | `rate_counters` shipped at L.2; per-workspace admission not built |
| S.3 Egress hardening at scale | #871 ✓ | 🔧 | the carry-in (address pinning) ✅ (#1108); deployment-wide provider budgets and the hostile-fake battery under load 📋 |
| S.4 Loop extraction + retention | — | 🔧 | `fn_retention_batch` exists and nothing reaches it (#1167); the published retention windows are unenforced (#1173) |
| S.5 Broad enablement + pre-check measurement | — | 📋 | `usage_precheck` flag exists, default off; no canary measurement |

**Tracker hygiene (2026-09-02):** #806, #942, #1014, #1183, #986 and #797 closed (their work had landed); #695 and #943 closed as moot under ruling 1; #806, #841, #751 and #942 retitled to their true state; #746, #790, #841 and #751 carry a current-state comment and stay open for their remainders.

## Codebase anchor (pass 4 — `main` @ `2e13f97`, 2026-08-03)

Internal consistency is not truth about the repo, so pass 4 verified every claim this plan makes about the current codebase — cited files, functions, columns, line references, counts, and "main does / does not do X" statements — against `main` @ `2e13f97` (the tip that merged the 2026-05-26 investigation), via independent verification passes over `00`–`07` — run twice: the original four-pass sweep, then (after that session was interrupted before persisting its evidence) a full fresh five-verifier re-verification whose claim-by-claim tables are the ones published on the PR. Where the plan and the code disagreed, the disagreement was recorded and resolved in the code's favor, in place, each marked "pass-4 anchor":

- **Meta publish cap 25 → 100** (`05` platform inputs; pinned against Meta's content-publishing doc — the one 0.4 item this pre-completes) with the derived arithmetic corrected.
- **W.4's attribution source**: `audit_log` never records account switches on `main` — the timeline reconstructs from `service_runs` (`switch_account` rows plus `add_account` activation records), gaps to quarantine (pass-4 increment W.4; the rule now lives at `04` M.1).
- **No JWT exists on `main`** — the consumer contract re-grounded on the real HMAC-signed token mechanism (pass-4 increment W.6, now X.2; `07` §1).
- **Encryption is already MultiFernet** under `ENCRYPTION_KEYS` — the plan adopts the shipped env name and credits the existing rotation machinery (`07` §3).
- **Three legacy timestamp columns are already `TIMESTAMPTZ`** — the naive-conversion rule now names its exception set (`02` §0/§9).
- Smaller corrections: three workspace config columns are new rather than carried, `media_sync_enabled`'s disposition added, no legacy "unsupported" media flag (it is a lock today), no stored chat titles (F.2 fallback corrected), landing deploys to Vercel not Railway, `/health` not `/healthz`, two missing `graph.facebook.com` sites added to FC-4's enumeration, the FC-1.4 hierarchy described as chat-mediated, the superseded-package line anchors re-pinned (+2 after their banners), and the Telegram-module baseline re-counted (76 at this anchor). Fresh-run refinements: W.4's history-writer surface named truly (`src/services/core/posting.py` on `main` is a vestigial Drive-alert utility, not the history writer), the recordless auto-set inventory corrected to two paths (`add_account` activations are reconstructible), the 200/user/hr figure's in-tree status stated precisely (second-hand citations exist), and the 25-per-hour guides drift tracked as #734.

Also verified clean: the D31 liveness premises (the corrupt-phrase classifier, the `auth_method` filter, `REFRESH_BUFFER_HOURS = 168`, the fail-open #705/#707 gate — all present on `main` exactly as cited), the 2026-05 investigation chain (the newer in-tree host-routing RCA supersedes the storage-corruption theory, which this plan never asserts), FC-4's zero-feature-loss greps, the 14-table inventory and the full `02` §9 legacy column mapping, the 004/008/010/034 chain defects, the 6 duplicate groups (recorded in-tree), and manual-mode/membership-sync current behavior. **Bounds, honestly:** production database state (constraint residue, row counts, live types), console facts (Google consent-screen status, Railway topology), and non-Meta external platform constraints were **not** verifiable from the repo — the plan's existing gates (`0.2`'s `\d`-against-prod precondition, `0.4`, the `03` owner items) carry exactly those. Claim-by-claim tables with per-verifier coverage bounds are in the pass-4 disposition comment on PR #731.

## Start here — authority, reading order, self-containment

**This directory is the authoritative plan.** Everything else under `documentation/planning/` that addresses multi-tenancy — in particular `../../archive/2026-07-29-high-throughput-multi-tenant/` (#722's package, of similar vintage) and `../../archive/2026-07-29-data-model-evaluation/` (#721's) — is **historical input, superseded by this plan**; each of their files carries a banner saying so, and both packages were moved to `documentation/archive/` on 2026-09-02. Do not implement from anything outside this directory.

**Reading order:** `00` (fixed constraints — nothing below may contradict them) → `01` (requirements ledger + architecture) → `02` (schema + state machines, executable DDL) → `03` (decision record — why, and what lost) → `04` (the increment sequence you execute) → `05` (every operational number) → `06` (product lifecycles + multi-account semantics) → `07` (security model). An implementer works increment-by-increment from `04`, reaching into the others as it cites them.

**Self-containment guarantee:** these nine files plus this repository's own code and migrations are sufficient — no external document is required to execute this plan. Citations to #721, #722 (EP/IP/SE/TT), #730 (RF-*), the fleet-side cold design, and review comments are **provenance for auditors**: every requirement, shape, number, gate, and absorbed exit criterion is restated in full where it is used. #721's package was merged in-tree on 2026-08-10 (now under `documentation/archive/`) but is not required reading — nothing here depends on it. The open items an implementer must know: **fork PA-1 (`03`) — build its default (a) until ruled; the email-provider ack (`03` pass-4 items) — build the EmailSender port with Resend as the swappable default until acked; the Google consent-screen publishing status (`03` pass-4 items) — an owner console action X.3's sign-in gate depends on.**

## What this is

Three design efforts targeted the same future system and disagreed: the data-model package (#721), the architecture package (#722, in-tree at `documentation/archive/2026-07-29-high-throughput-multi-tenant/`), and an independently-derived cold design (fleet-side). A cross-check adjudicated them (verdict: incompatible as written — 5 blocking conflicts at that check's grain; review A's A–P inventory is the same incompatibility counted finer, `03` count-grain note), the ratified review findings (#730, `review-findings.md` in the same package dir) amended #722, and the product owner issued rulings (2026-08-02) that fix the tenancy model, the interaction-layer direction, the media-credential model, and the Instagram auth path.

This plan is the single consolidated output. Its spine is the cold design — the only input whose every default traces to a stated requirement — with #722's engineering and #721's domain-model direction pulled in where they earn their place, and the 2026-08-02 rulings applied as fixed constraints.

## Inputs and provenance

| Input | Where | What survives here |
|---|---|---|
| Product rulings 2026-08-02 | `00-fixed-constraints.md` (normative restatement) | All of it — FC-0..FC-9 are not revisable by implementers |
| Envelope ruling (prior) | #730 discussion | Thousands of provisioned tenants; sets C2 topology and all numbers |
| #722 architecture package | `../../archive/2026-07-29-high-throughput-multi-tenant/` (`epic.md`, `implementation-plan.md`, `self-evaluation.md`, `tiered-issue-triage.md`) | Jobs/leases/outbox semantics, webhook ingress, RLS harness discipline, P0 exit-gate rigor — as re-based by `04` |
| #730 ratified findings | `../../archive/2026-07-29-high-throughput-multi-tenant/review-findings.md` | R1–R5 all honored; mapping noted per decision in `03` |
| #721 data-model package | `../../archive/2026-07-29-data-model-evaluation/` (merged in-tree 2026-08-10 as PR #721; archived 2026-09-02) | Intent-ledger direction, workspace-rooted tenancy direction, six-stage migration machine, consumer-contract track — letter re-derived (24 recorded quality flags) |
| Cold design | fleet-side working set (externally held, **not required reading**); its requirement set R1–R8 / T1–T4 / H1–H6 is restated in full in `01` §Requirements ledger (the only normative home) | The requirements ledger and the overall shape of `01`/`02` |
| IG platform reference | fleet vault doc (externally held, **not required reading** — every platform fact it contributed is restated in `05` and re-verified against Meta's primary documentation at `04` 0.4) | the publish cap (its 25/rolling-24h was stale — corrected to 100 at the pass-4 anchor against Meta's primary doc, `05`), 200/user/hr, Instagram-Login-vs-Facebook-Login split, App Review lead times |

## File map

- `00-fixed-constraints.md` — product-owner rulings as normative requirements. Read first; nothing below may contradict them.
- `01-target-architecture.md` — the requirements ledger (R/T/H ids every other file cites), process roles, interaction-layer port, tenancy spine, job machinery.
- `02-domain-model.md` — full schema, state machines with complete transition matrices and failure terminals, uniqueness keys, RLS.
- `03-decision-record.md` — every contested decision: what was decided, the requirement it serves, what it supersedes, reversibility.
- `04-execution-sequence.md` — the single consolidated increment sequence (supersedes both packages' phase plans), with exit gates and traceability to #722's P0/P1 items and #730's Rs.
- `05-operational-numbers.md` — initial values for every operational setting (the package-level gap both priors left open), with derivations, retention/DR tables, and the revision rule.
- `06-product-lifecycles.md` — workspace/membership lifecycles, multi-account scheduling semantics, account movement, customer-visible failure behavior, named non-goals.
- `07-security-model.md` — web sign-in (Google OIDC + sessions), OAuth state binding (connect/reconnect/signin/link + the start-token door), credential encryption + key rotation, audit integrity, oracle/log hygiene, first-party API auth.

## Authority and supersession

The 2026-07-29 package is **superseded in full** — where it and this plan say different things, this plan is not merely preferred, it is the only authority. The package remains in-tree solely as the evidence trail (the reviews cite it); every file in it carries a supersession banner. `04-execution-sequence.md` names, per increment, which package items were absorbed — as audit annotations, with the absorbed scope and gates restated in full in `04` itself. The ratified #730 findings are honored in full; each R is anchored to its consolidated home in `03-decision-record.md` (its file lives in the superseded directory but is itself *honored*, not superseded — its banner says exactly that). #721's package is in-tree only as an archived evidence trail and is not needed: everything load-bearing was restated here (verified — its five load-bearing quality flags resolve to FLAG-1/2/3/7/10 fixes named in `02`/`03`; nothing else was depended on).
